Algoritmo "EXERCICIO 2"
// calcular a área de um triângulo

Var

area, base, altura : real

Inicio

escreval (" Qual é a base do triangulo ?")
 leia (base)
  escreval()
escreval (" Qual é a altura do triangulo ?")
 leia (altura)
  escreval()
 area <- (base*altura)/2
  escreval (" A área do triangulo é,", area, ".")


Fimalgoritmo